<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_quick_log_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinAndroidAppTemplate">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="stateHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.minandroidapp.ui.tag.TagManagerActivity"
            android:exported="false" />

        <activity
            android:name="com.example.minandroidapp.ui.entries.EntriesOverviewActivity"
            android:exported="false" />

        <activity
            android:name="com.example.minandroidapp.settings.SettingsActivity"
            android:exported="false" />
    </application>

</manifest>
